# =============================================================
# Taller Sumativo – Despliegue de entorno con Docker
# Asignatura: Aplicaciones y Tecnologías de la Web
# Universidad San Sebastián
#
# Arquitectura de 3 capas (cliente-servidor en contenedores):
#   - frontend : HTML estático servido por Nginx  → localhost:8080
#   - backend  : API REST con Python/Flask         → localhost:3000
#   - db       : PostgreSQL con volumen persistente
# =============================================================

version: '3.8'

services:
  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - taller-network
    environment:
      - NGINX_HOST=tu-app.fly.dev  # Tu dominio en Fly

  backend:
    build: ./backend
    ports:
      - "3000:5000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      - taller-network
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=postgresql://postgres:${DB_PASSWORD}@db:5432/${POSTGRES_DB}
      - FLASK_APP=app.py

  db:
    image: postgres:16-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - taller-network
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

networks:
  taller-network:
    driver: bridge
